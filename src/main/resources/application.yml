spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jobscheduler
    username: postgres
    password: Sid@8551871204
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

server:
  port: 8080